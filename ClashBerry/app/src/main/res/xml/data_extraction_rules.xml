<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- Exclude all files in the cache directories -->
        <exclude domain="cache" />
    </cloud-backup>
    <device-transfer>
        <!-- Exclude all files in the cache directories -->
        <exclude domain="cache" />
    </device-transfer>
</data-extraction-rules>